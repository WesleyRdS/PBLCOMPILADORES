                case ".": #se um ponto for lido significa que encontrou um delimitador
                    if (self.simboloAtual != ""):
                        self.reservada.append(self.linha_lida)
                        self.reservada.append("IDE") #classifica como identificador
                        self.reservada.append(self.simboloAtual)
                        self.simbolos.append(self.reservada)
                    self.resetReservada()
                    self.resetSimbAtual()
                    self.ponto_state() #muda para o estado de ponto flutuante

                case ",":  # se uma virgula for lida significa que encontrou um delimitador
                    if (self.simboloAtual != ""):
                        self.reservada.append(self.linha_lida)
                        self.reservada.append("IDE")  # classifica como identificador
                        self.reservada.append(self.simboloAtual)
                        self.simbolos.append(self.reservada)
                    self.resetReservada()
                    self.resetSimbAtual()
                    self.ponto_state()  # muda para o estado de ponto flutuante

                case ";":  # se um ponto e virgula for lido significa que encontrou um delimitador
                    if (self.simboloAtual != ""):
                        self.reservada.append(self.linha_lida)
                        self.reservada.append("IDE")  # classifica como identificador
                        self.reservada.append(self.simboloAtual)
                        self.simbolos.append(self.reservada)
                    self.resetReservada()
                    self.resetSimbAtual()
                    self.ponto_state()  # muda para o estado de ponto flutuante



                    case "/": #se o valor lido for uma barra significa que vai ter um operador aritimetico ou comentario
                    if (self.simboloAtual != ""):
                        self.reservada.append(self.linha_lida)
                        self.reservada.append("IDE") #classifica como um identificador desconsiderando a  barra
                        self.reservada.append(self.simboloAtual)
                        self.simbolos.append(self.reservada)
                    self.resetReservada()
                    self.resetSimbAtual()
                    self.pularProximoSimbolo() #vai pro proximo simbolo
                    self.ignoraBranco()
                # se o valor lido for um simbolo de +, - ou * significa que vai ter um operador aritimetico
                case "+":
                    if (self.simboloAtual != ""):
                        self.reservada.append(self.linha_lida)
                        self.reservada.append("IDE")
                        self.reservada.append(self.simboloAtual)
                        self.simbolos.append(self.reservada)
                    self.resetReservada()
                    self.resetSimbAtual()
                    self.pularProximoSimbolo()
                    self.ignoraBranco()

                case "-":
                    if (self.simboloAtual != ""):
                        self.reservada.append(self.linha_lida)
                        self.reservada.append("IDE")
                        self.reservada.append(self.simboloAtual)
                        self.simbolos.append(self.reservada)
                    self.resetReservada()
                    self.resetSimbAtual()
                    self.pularProximoSimbolo()
                    self.ignoraBranco()

                case "*":
                    if (self.simboloAtual != ""):
                        self.reservada.append(self.linha_lida)
                        self.reservada.append("IDE")
                        self.reservada.append(self.simboloAtual)
                        self.simbolos.append(self.reservada)
                    self.resetReservada()
                    self.resetSimbAtual()
                    self.pularProximoSimbolo()
                    self.ignoraBranco()

                case "(":
                    if (self.simboloAtual != ''):
                        self.reservada.append(self.linha_lida)
                        self.reservada.append("IDE")
                        self.reservada.append(self.simboloAtual)
                        self.simbolos.append(self.reservada)
                    self.resetReservada()
                    self.resetSimbAtual()
                    self.pularProximoSimbolo()
                    self.ignoraBranco()
                case ")":
                    if (self.simboloAtual != ''):
                        self.reservada.append(self.linha_lida)
                        self.reservada.append("IDE")
                        self.reservada.append(self.simboloAtual)
                        self.simbolos.append(self.reservada)
                    self.resetReservada()
                    self.resetSimbAtual()
                    self.pularProximoSimbolo()
                    self.ignoraBranco()

                case "[":
                    if (self.simboloAtual != ''):
                        self.reservada.append(self.linha_lida)
                        self.reservada.append("IDE")
                        self.reservada.append(self.simboloAtual)
                        self.simbolos.append(self.reservada)
                    self.resetReservada()
                    self.resetSimbAtual()
                    self.pularProximoSimbolo()
                    self.ignoraBranco()
                case "]":
                    if (self.simboloAtual != ''):
                        self.reservada.append(self.linha_lida)
                        self.reservada.append("IDE")
                        self.reservada.append(self.simboloAtual)
                        self.simbolos.append(self.reservada)
                    self.resetReservada()
                    self.resetSimbAtual()
                    self.pularProximoSimbolo()
                    self.ignoraBranco()

                case "{":
                    if (self.simboloAtual != ''):
                        self.reservada.append(self.linha_lida)
                        self.reservada.append("IDE")
                        self.reservada.append(self.simboloAtual)
                        self.simbolos.append(self.reservada)
                    self.resetReservada()
                    self.resetSimbAtual()
                    self.pularProximoSimbolo()
                    self.ignoraBranco()
                case "}":
                    if (self.simboloAtual != ''):
                        self.reservada.append(self.linha_lida)
                        self.reservada.append("IDE")
                        self.reservada.append(self.simboloAtual)
                        self.simbolos.append(self.reservada)
                    self.resetReservada()
                    self.resetSimbAtual()
                    self.pularProximoSimbolo()
                    self.ignoraBranco()
